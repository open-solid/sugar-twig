{#
    Card Footer component - shadcn/ui port

    Usage:
        <CardFooter>
            <Button variant="outline">Cancel</Button>
            <Button>Submit</Button>
        </CardFooter>

    Parameters:
        className - Additional CSS classes
#}
<div data-slot="card-footer" {{ html_attrs({class: 'flex items-center rounded-b-xl border-t bg-muted/50 p-4 group-data-[size=sm]/card:p-3'}) }}>
    {%- block content %}{% endblock -%}
</div>
